version: "1.5"
namespace:
    key: default
    name: Default
flags:
    - key: email_v2
      name: email_v2
      type: VARIANT_FLAG_TYPE
      description: send email using mail-sender
      enabled: true
segments:
    - key: by-tenant-id
      name: by tenant ID
      match_type: ALL_MATCH_TYPE
